# 验证记录

v1.0.1 于 2026-09-22 在 Windows 本地完成以下验证：

- 18 项技能通过本仓库结构检查，以及本地 Codex skill-creator 的 `quick_validate.py` 检查：名称、描述、界面元数据、引用路径和资源可发现性均通过。
- 16 项自动化测试通过：3 项安装测试、13 项项目记录测试。覆盖安装不覆盖已有内容、复制一致性、只读操作、中文身份、历史修正、损坏记录、跨文件证据冲突、跨进程并发写入及锁释放。
- 手工 JSON 记录可被脚本读取并继续修正。并发写入同一证据编号的不同内容时，冲突会被拒绝，已有记录保留。
- 已复核文档中的示例事实和路演估时口径；示例假设不得变成用户事实，稿件估时不代表真实试讲结果。

前身版本的行为试跑覆盖 7 个原始场景，其中 3 组同题对照，另有 3 个新场景复核。它们用于发现交付、数字口径和资源约束方面的问题，不能证明在所有模型和宿主上均有效，也不支持“普遍优于 dbs”的结论。

## 在本机复现

使用 Python 3.10 或更高版本，在仓库根目录运行：

```bash
python -m pip install -r requirements-dev.txt
python scripts/validate_suite.py
python -m unittest discover -s scripts -p "test_*.py" -v
python scripts/build_release.py
```

前三项分别安装开发依赖、验证技能结构、运行行为回归测试。打包脚本仅收录发布目录与指定文件，并回读 ZIP 核对内容。

GitHub Actions 配置会在 Windows 和 Linux 上运行上述检查。只有对应提交的工作流成功后，才能将该平台记为通过；提交配置本身不等于测试通过。模型输出和宿主实际加载效果仍需在各自环境中检验。
